import 'package:live_sensors/snapshot.dart';

class ApiClient {
  // Future<void> login() await {

  // }

  Future<void> sendSnapshot(Snapshot snap) {
    return Future.delayed(const Duration(seconds: 3));
  }
}